version: '3.8'
services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PROXY_BASE_URL=${PROXY_BASE_URL}
      - PROXY_AUTH_USER=${PROXY_AUTH_USER}
      - PROXY_AUTH_PW=${PROXY_AUTH_PW}
